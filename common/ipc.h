// Inter Process Communicaiton

// Server to Client IPC
enum s2c_ipc
{
    s2c_ipc_noop, // Does nothing (used to check if pipe is closed)
};

// Client to Server IPC
enum c2s_ipc
{
    c2s_ipc_noop, // Does nothing (used to check if pipe is closed)
};
